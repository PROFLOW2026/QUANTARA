# Broker Replay V3 — 2026-09-11

Historical read-only replay generated by `replay_historical_day()`.

Run locally (requires DATABASE_URL + competition history):

```bash
cd apps/engine
python -c "
from datetime import datetime, timezone
from quantara_engine.db.session import session_scope
from quantara_engine.persistence.store import TradingStore
from quantara_engine.broker.replay_v3 import write_replay_v3_audit_markdown
d0 = datetime(2026, 9, 11, tzinfo=timezone.utc)
d1 = datetime(2026, 9, 12, tzinfo=timezone.utc)
with session_scope() as s:
    write_replay_v3_audit_markdown(TradingStore(s), '../docs/audits/2026-09-11-BROKER-REPLAY-V3.md', d0, d1)
"
```

## Reconciliation targets

| Metric | Target |
|--------|--------|
| Financial | 0.00 |
| Margin | 0.00 |
| Attribution | 0.00 |
| Physical quantity diff | 0 |
| Orphan exits executed | 0 |

Results are populated when the replay script runs against live DB history.
