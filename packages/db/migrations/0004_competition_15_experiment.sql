-- Archive legacy 5-portfolio experiment metadata (no data deletion).
-- Active experiment is seeded via scripts/seed_competition.py (experiment ...0200).

-- Ensure experiment status enum supports archived runs (completed already exists).
-- No schema change required; seed script sets legacy experiment to 'completed'.
