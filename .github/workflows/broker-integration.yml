name: Broker Integration Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  broker-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install engine dependencies
        run: pip install -e "./apps/engine[dev]"

      - name: Run broker integration tests
        env:
          BROKER_TEST_ADMIN_URL: postgresql://postgres:postgres@postgres:5432/postgres
          BROKER_TEST_DATABASE_URL: postgresql://quantara:quantara@postgres:5432/quantara_broker_test
        working-directory: apps/engine
        run: pytest tests/test_broker_execution_integration.py -v --tb=short
